apiVersion: metalk8s.scality.com/v1alpha1
kind: DiscoveredDisk
metadata:
  labels:
    app.kubernetes.io/name: platform-disk-management-agent
    app.kubernetes.io/managed-by: kustomize
  name: discovereddisk-sample
spec:
  # TODO(user): Add fields here
